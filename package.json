{
  "name": "astro-blog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "cross-env NODE_ENV=development astro dev",
    "dev:test": "cross-env NODE_ENV=test astro dev",
    "build:test": "astro check && tsc --noEmit && cross-env NODE_ENV=test astro build",
    "build:prod": "astro check && tsc --noEmit && cross-env NODE_ENV=production astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "engines": {
    "node": ">=18.17.0",
    "pnpm": ">=7.8.0"
  },
  "dependencies": {
    "astro": "^3.1.2",
    "vite": "^4.4.9"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.50.0",
    "eslint-plugin-astro": "^0.29.0",
    "prettier": "^3.0.3",
    "prettier-plugin-astro": "^0.12.0"
  }
}